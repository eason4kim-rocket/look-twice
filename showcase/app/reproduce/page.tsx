"use client";
import {SiteShell,useLanguage} from "../components/SiteShell";
import "./reproduce.css";
export default function ReproducePage(){return <SiteShell><Reproduce/></SiteShell>}
function Reproduce(){const{language}=useLanguage();const zh=language==="zh";return <main><header className="page-header"><div><span className="eyebrow">REPLAY-FIRST REPRODUCTION</span><h1>{zh?"无需 GPU，也能审计闭环。":"Audit the loop without a GPU."}</h1></div><p>{zh?"公开网站只播放已记录的 AMD GPU 证据。Docker 不依赖 Genesis、ROCm 或私有 Purify。":"The public site replays recorded AMD GPU evidence. Docker needs no Genesis, ROCm, private Purify code or live cloud instance."}</p></header><section className="repro-grid"><article><span>01 / START</span><h2>One command</h2><pre><code>docker compose up --build</code></pre><p>{zh?"打开 http://localhost:3000。Replay 在 CPU 环境完整运行。":"Open http://localhost:3000. Evidence replay runs in a CPU-only environment."}</p></article><article><span>02 / VERIFY</span><h2>Frozen SHA guard</h2><pre><code>python3 scripts/verify_frozen_foundation.py</code></pre><p>{zh?"检查 V8 runtime、双校准、Purify binary、episode 与公共 bundle。":"Checks frozen runtime, both calibration artifacts, Purify binary, episodes and public bundles."}</p></article><article><span>03 / INSPECT</span><h2>Raw evidence</h2><pre><code>showcase/public/data/replays/*.json</code></pre><p>{zh?"每个结论均可追溯到 Claim、capture root、GateReceipt 和源 episode SHA。":"Every claim links to capture roots, GateReceipts and the source episode SHA."}</p></article></section><section className="boundary"><span>BOUNDARY</span><h2>{zh?"比赛参考实现，不是安全认证。":"Contest reference implementation, not a safety certification."}</h2><div><p>✓ Recorded Genesis + AMD GPU evidence</p><p>✓ Frozen model and conformal identities</p><p>✓ Purify Go authorization receipts</p><p>× No real-robot claim</p><p>× No live GPU website dependency</p><p>× No complete private Purify product</p></div></section></main>}
